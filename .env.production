# Live honeypot stats API — Cloudflare Worker (push model).
# Public read-only URL, safe to commit. The dashboard fetches this at runtime;
# unset (e.g. local dev) it falls back to the bundled mock at /data/honeypot-stats.json.
NEXT_PUBLIC_HONEYPOT_STATS_URL=https://honeypot-stats.valdeniomarinho.workers.dev/stats
