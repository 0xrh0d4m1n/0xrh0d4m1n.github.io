# Next.js
.next/
out/
*.tsbuildinfo

# Node
node_modules/
npm-debug.log*
.yarn-error.log

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# AI / Agentic workflow (nunca commitar memória, tasks, estado de agente, regras locais)
.claude/
CLAUDE.md
CLAUDE.local.md
.cursor/
.cursorrules
.windsurfrules
.aider*
.continue/
.github/copilot-instructions.md
AGENTS.md
.agent/
.ai/
tasks/

# Env — .env.production é intencional (só NEXT_PUBLIC, URL pública)
.env
.env.*
!.env.example
!.env.production

# Secrets / chaves — rede de segurança para `git add .` (NUNCA versionar)
*.pem
*.key
*.p12
*.pfx
*.ppk
*.kdbx
*.keystore
id_rsa*
id_ed25519*
*_rsa
*.ovpn
secrets.*
service-account*.json
credentials*.json

# Playwright
tests/.output/
tests/playwright-report/
.playwright/
/tasks/

# Estado local do Cloudflare Wrangler. Ele NASCE no primeiro `wrangler dev`/`deploy` e
# guarda cache e estado de sessao — nada disso pertence a um repo PUBLICO.
# ⚠️ Entra aqui ANTES de existir, de proposito: o habito da casa e `git add .`, e a regra
# que so e escrita depois do arquivo aparecer chega tarde por definicao.
.wrangler/
